import { departmentsInfo } from "@/constants/departments"; // Adjust the path according to your project structure

const departments = departmentsInfo;

export default function Departments() {
  return (
    // TODO: Add the departments section here with the data from the departments constant and this will be a stacking card component with the department name and description and this will animate on scroll. I mean department card will animate when it comes into the view and stack on top of each other. You can use gsap for this animation.
    "OK"
  );
}
